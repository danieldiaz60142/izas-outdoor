<style>
    [newsletter-layout].section.newsletter{
        margin: 0;
        padding: 40px 0 40px;
    }
    [newsletter-layout].section.newsletter .section__block--heading{
        margin-bottom: 22px;
        letter-spacing: 0.2px;
        font-weight: 600;
        font-family: Archivo;
    }
    [newsletter-layout].section.newsletter .section__block.rte{
        margin-bottom: 28px;
        padding: 0 44px;
    }
    [newsletter-layout].section.newsletter .section__block.rte p {
        font-weight: 300;
        font-size: 13px;
        letter-spacing: -0.1px;
    }

    [newsletter-layout].section.newsletter .section__block.form-width .form__field{
        padding: 0 8px;
    }
    [newsletter-layout].section.newsletter .input-with-button input{
        padding: 17px 16px;
        font-weight: 300;
        font-size: 13px;
    }
    [newsletter-layout].section.newsletter .input-with-button input::placeholder{
        font-weight: 300;
        font-size: 13px;
    }
    [newsletter-layout].section.newsletter .input-with-button .btn:not(.btn--primary){
        padding-right: 17px;
    }
    @media (min-width: 769px) {
        [newsletter-layout].section.newsletter{
            margin: 0;
            padding: 51px 0 50px;
        }
        [newsletter-layout].section.newsletter .section__block--heading{
            margin-bottom: 15px;
            letter-spacing: 0.2px;

        }
        [newsletter-layout].section.newsletter .section__block.rte{
            margin-bottom: 22px;
            padding: 0;
        }
        [newsletter-layout].section.newsletter .section__block.rte p{
            font-weight: 300;
            font-size: 16px;
            letter-spacing: -0.1px;
        }
        [newsletter-layout].section.newsletter .section__block.form-width .form__field{
            padding: 0 25px;
        }
        [newsletter-layout].section.newsletter .input-with-button input{
            padding: 17px 20px;
            font-weight: 300;
            font-size: 14px;
        }
        [newsletter-layout].section.newsletter .input-with-button input::placeholder{
            font-weight: 300;
            font-size: 14px;
        }
    }
</style>