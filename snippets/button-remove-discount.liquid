<style>
    .btn-remove {
        margin: -10px;
        padding: 10px;
        color: rgb(var(--heading-color));
    }
</style>
<button type="button" class="btn-remove">
    <svg width="24" height="24" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5" fill="none"
         fill-rule="evenodd" stroke-linejoin="round" aria-hidden="true" focusable="false" role="presentation"
         class="icon">
        <path d="M5 19 19 5M5 5l14 14"></path>
    </svg>
</button>

<script>
    // document.addEventListener('DOMContentLoaded', function () {
    //     let remove;
    //
    //     remove = document.querySelectorAll('.btn-remove');
    //     remove.forEach(function (btnRemove) {
    //         btnRemove.addEventListener('click', function () {
    //             localStorage.removeItem("storedDiscount");
    //
    //             fetch("/discount/null")
    //                 .then(function (response) {
    //                     return response;
    //                 })
    //                 .then(function (data) {
    //                     if (data.status === 200) {
    //                         document.dispatchEvent(new CustomEvent('dispatch:cart-drawer:refresh', {
    //                             bubbles: true
    //                         }));
    //                         fetch("/cart.js").then(result => result.json()).then(result => {
    //                         });
    //                     } else {
    //                         alert('Something wrong. Please try again.');
    //                     }
    //                 });
    //             fetch("/checkout?discount=%20");
    //         })
    //     })
    // });
</script>